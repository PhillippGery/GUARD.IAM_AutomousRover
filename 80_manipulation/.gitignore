# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Envs ---
.venv/
venv/
env/

# --- ML artifacts (too large / machine-specific — never commit) ---
outputs/                # training runs + checkpoints
*.safetensors
*.ckpt
*.pt
*.mp4                   # recorded episode videos
wandb/

# --- Datasets & caches (live in ~/.cache/huggingface/lerobot) ---
.cache/
datasets_cache/

# --- Local / machine-specific ---
home_action.json        # per-robot home pose (regenerate locally)
*.log
frames_*.gv
frames_*.pdf

# --- OS / editor ---
.DS_Store
.vscode/
.idea/
